# 数值分析实习一解答

已知线性方程组

```text
      |10  7  8  7|
      | 7  5  6  5|
A  =  | 8  6 10  9| ,
      | 7  5  9 10|

b      = (32, 23, 33, 31)^T
b_hat  = (32.01, 22.99, 33.01, 30.99)^T
```

要求：

1. 求解 `Ax = b`；
2. 把 `b` 扰动为 `b_hat` 后，再求解 `A x_hat = b_hat`；
3. 将计算得到的实际相对误差与误差估计量进行比较；
4. 分别使用 `1` 范数和 `∞` 范数；
5. 对所得结果的可靠性进行评估。

下面分别用列主元高斯消去法和高斯-赛德尔迭代法求解。

## 1. 列主元高斯消去法求解 `Ax = b`

增广矩阵为

```text
|10  7  8  7 | 32|
| 7  5  6  5 | 23|
| 8  6 10  9 | 33|
| 7  5  9 10 | 31|
```

### 第 1 步消元

第 1 列中绝对值最大的元素为 `10`，故第 1 行作为主元行，不必交换。

```text
m21 = 7/10,  m31 = 8/10 = 4/5,  m41 = 7/10
```

消元后得到

```text
|10   7    8    7   | 32  |
| 0  1/10  2/5  1/10| 3/5 |
| 0  2/5  18/5 17/5 | 37/5|
| 0  1/10 17/5 51/10| 43/5|
```

### 第 2 步消元

第 2 列中，第 2 到第 4 行对应元素分别为 `1/10, 2/5, 1/10`，绝对值最大的为 `2/5`，故交换第 2、3 行。

交换后：

```text
|10   7    8    7   | 32  |
| 0  2/5  18/5 17/5 | 37/5|
| 0  1/10  2/5 1/10 | 3/5 |
| 0  1/10 17/5 51/10| 43/5|
```

消元因子为

```text
m32 = (1/10)/(2/5) = 1/4
m42 = (1/10)/(2/5) = 1/4
```

消元后得到

```text
|10  7    8    7   | 32   |
| 0  2/5 18/5 17/5 | 37/5 |
| 0   0  -1/2 -3/4 | -5/4 |
| 0   0   5/2 17/4 | 27/4 |
```

### 第 3 步消元

第 3 列中，第 3、4 行对应元素为 `-1/2` 和 `5/2`，绝对值最大的为 `5/2`，故交换第 3、4 行。

交换后：

```text
|10  7    8    7   | 32   |
| 0  2/5 18/5 17/5 | 37/5 |
| 0   0   5/2 17/4 | 27/4 |
| 0   0  -1/2 -3/4 | -5/4 |
```

消元因子为

```text
m43 = (-1/2)/(5/2) = -1/5
```

消元后得到上三角方程组

```text
|10  7    8    7   | 32   |
| 0  2/5 18/5 17/5 | 37/5 |
| 0   0   5/2 17/4 | 27/4 |
| 0   0    0  1/10 | 1/10 |
```

### 回代求解

由第 4 行得

```text
(1/10)x4 = 1/10
```

所以

```text
x4 = 1
```

由第 3 行得

```text
(5/2)x3 + (17/4)x4 = 27/4
```

代入 `x4 = 1` 得

```text
(5/2)x3 = 27/4 - 17/4 = 10/4 = 5/2
```

所以

```text
x3 = 1
```

由第 2 行得

```text
(2/5)x2 + (18/5)x3 + (17/5)x4 = 37/5
```

代入 `x3 = x4 = 1` 得

```text
(2/5)x2 = 37/5 - 18/5 - 17/5 = 2/5
```

所以

```text
x2 = 1
```

由第 1 行得

```text
10x1 + 7x2 + 8x3 + 7x4 = 32
```

代入 `x2 = x3 = x4 = 1` 得

```text
10x1 = 32 - 7 - 8 - 7 = 10
```

所以

```text
x1 = 1
```

因此

```text
x = (1, 1, 1, 1)^T
```

## 2. 列主元高斯消去法求解 `A x_hat = b_hat`

此时只改变右端项，矩阵 `A` 不变，所以消元过程与前面完全一致。

对应的增广矩阵为

```text
|10  7  8  7 | 32.01|
| 7  5  6  5 | 22.99|
| 8  6 10  9 | 33.01|
| 7  5  9 10 | 30.99|
```

经过与前面相同的列主元消去后，得到上三角方程组

```text
|10  7    8    7   | 3201/100 |
| 0  2/5 18/5 17/5 | 3701/500 |
| 0   0   5/2 17/4 | 2693/400 |
| 0   0    0  1/10 | 79/1000  |
```

回代：

由第 4 行得

```text
x4_hat = (79/1000)/(1/10) = 79/100 = 0.79
```

由第 3 行得

```text
(5/2)x3_hat + (17/4)x4_hat = 2693/400
```

代入 `x4_hat = 79/100`，得

```text
x3_hat = 27/20 = 1.35
```

由第 2 行得

```text
(2/5)x2_hat + (18/5)x3_hat + (17/5)x4_hat = 3701/500
```

解得

```text
x2_hat = -9/25 = -0.36
```

由第 1 行得

```text
10x1_hat + 7x2_hat + 8x3_hat + 7x4_hat = 3201/100
```

解得

```text
x1_hat = 91/50 = 1.82
```

因此

```text
x_hat = (91/50, -9/25, 27/20, 79/100)^T
      = (1.82, -0.36, 1.35, 0.79)^T
```

从而

```text
delta_x = x_hat - x = (0.82, -1.36, 0.35, -0.21)^T
delta_b = b_hat - b = (0.01, -0.01, 0.01, -0.01)^T
```

## 3. 高斯-赛德尔迭代法求解 `Ax = b`

将原方程组写成迭代格式：

```text
x1 = (32 - 7x2 - 8x3 - 7x4)/10
x2 = (23 - 7x1 - 6x3 - 5x4)/5
x3 = (33 - 8x1 - 6x2 - 9x4)/10
x4 = (31 - 7x1 - 5x2 - 9x3)/10
```

高斯-赛德尔迭代格式为

```text
x1^(k+1) = (32 - 7x2^(k)   - 8x3^(k)   - 7x4^(k)  ) / 10
x2^(k+1) = (23 - 7x1^(k+1) - 6x3^(k)   - 5x4^(k)  ) / 5
x3^(k+1) = (33 - 8x1^(k+1) - 6x2^(k+1) - 9x4^(k)  ) / 10
x4^(k+1) = (31 - 7x1^(k+1) - 5x2^(k+1) - 9x3^(k+1)) / 10
```

取初值

```text
x^(0) = (0, 0, 0, 0)^T
```

### 前几步迭代展开

第 1 次迭代：

```text
x1^(1) = 32/10 = 3.2
x2^(1) = (23 - 7*3.2)/5 = 0.12
x3^(1) = (33 - 8*3.2 - 6*0.12)/10 = 0.668
x4^(1) = (31 - 7*3.2 - 5*0.12 - 9*0.668)/10 = 0.1988
```

故

```text
x^(1) = (3.2, 0.12, 0.668, 0.1988)^T
```

第 2 次迭代：

```text
x1^(2) = (32 - 7*0.12 - 8*0.668 - 7*0.1988)/10 = 2.44244
x2^(2) = (23 - 7*2.44244 - 6*0.668 - 5*0.1988)/5 = 0.180184
x3^(2) = (33 - 8*2.44244 - 6*0.180184 - 9*0.1988)/10 = 1.0590176
x4^(2) = (31 - 7*2.44244 - 5*0.180184 - 9*1.0590176)/10 = 0.34708416
```

故

```text
x^(2) = (2.44244, 0.180184, 1.0590176, 0.34708416)^T
```

第 3 次迭代：

```text
x^(3) = (1.983698208, 0.2049172288, 1.27771535232, 0.459008822912)^T
```

### 前 10 次迭代结果

取误差量 `||x^(k) - x^(k-1)||_inf`，并同时记录残差 `||Ax^(k)-b||_inf`。

| k | `x1^(k)` | `x2^(k)` | `x3^(k)` | `x4^(k)` | `||x^(k)-x^(k-1)||_inf` | `||Ax^(k)-b||_inf` |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 3.200000 | 0.120000 | 0.668000 | 0.198800 | 3.200000 | 7.575600 |
| 2 | 2.442440 | 0.180184 | 1.059018 | 0.347084 | 0.757560 | 4.587418 |
| 3 | 1.983698 | 0.204917 | 1.277715 | 0.459009 | 0.458742 | 2.706187 |
| 4 | 1.713079 | 0.209421 | 1.390776 | 0.544436 | 0.270619 | 1.533999 |
| 5 | 1.559680 | 0.203082 | 1.440415 | 0.610310 | 0.153400 | 0.813859 |
| 6 | 1.478294 | 0.191581 | 1.453138 | 0.661580 | 0.081386 | 0.461432 |
| 7 | 1.440277 | 0.178267 | 1.445396 | 0.701816 | 0.040236 | 0.362124 |
| 8 | 1.427625 | 0.165034 | 1.427245 | 0.733625 | 0.031809 | 0.286279 |
| 9 | 1.429143 | 0.152881 | 1.404695 | 0.758934 | 0.025309 | 0.227784 |
| 10 | 1.437973 | 0.142269 | 1.381219 | 0.779187 | 0.023476 | 0.182275 |

可以看到迭代在逐步收敛，但速度较慢。

### 收敛性说明

高斯-赛德尔迭代矩阵记为 `B_GS = -(D+L)^(-1)U`。计算得其谱半径

```text
rho(B_GS) ≈ 0.996901 < 1
```

因此迭代法理论上收敛，但由于谱半径非常接近 `1`，所以收敛较慢。

取停止准则

```text
||x^(k) - x^(k-1)||_inf < 10^(-8)
```

则在 `k = 4062` 次迭代后得到

```text
x ≈ (1.00000195, 0.99999679, 1.00000080, 0.99999952)^T
```

残差为

```text
||Ax-b||_inf ≈ 6.047695e-8
```

这与列主元高斯消去法得到的精确解

```text
x = (1, 1, 1, 1)^T
```

一致。

## 4. 高斯-赛德尔迭代法求解 `A x_hat = b_hat`

扰动后的迭代格式为

```text
x1^(k+1) = (32.01 - 7x2^(k)   - 8x3^(k)   - 7x4^(k)  ) / 10
x2^(k+1) = (22.99 - 7x1^(k+1) - 6x3^(k)   - 5x4^(k)  ) / 5
x3^(k+1) = (33.01 - 8x1^(k+1) - 6x2^(k+1) - 9x4^(k)  ) / 10
x4^(k+1) = (30.99 - 7x1^(k+1) - 5x2^(k+1) - 9x3^(k+1)) / 10
```

仍取初值

```text
x^(0) = (0, 0, 0, 0)^T
```

### 前几步迭代展开

第 1 次迭代：

```text
x1^(1) = 32.01/10 = 3.201
x2^(1) = (22.99 - 7*3.201)/5 = 0.1166
x3^(1) = (33.01 - 8*3.201 - 6*0.1166)/10 = 0.67024
x4^(1) = (30.99 - 7*3.201 - 5*0.1166 - 9*0.67024)/10 = 0.196784
```

故

```text
x^(1) = (3.201, 0.1166, 0.67024, 0.196784)^T
```

第 2 次迭代：

```text
x^(2) = (2.4454392, 0.17331312, 1.063555168, 0.3433363488)^T
```

第 3 次迭代：

```text
x^(3) = (1.98850123744, 0.194495717184, 1.2844988658176, 0.4537522959641603)^T
```

### 前 10 次迭代结果

| k | `x1^(k)` | `x2^(k)` | `x3^(k)` | `x4^(k)` | `||x^(k)-x^(k-1)||_inf` | `||Ax^(k)-b_hat||_inf` |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 3.201000 | 0.116600 | 0.670240 | 0.196784 | 3.201000 | 7.555608 |
| 2 | 2.445439 | 0.173313 | 1.063555 | 0.343336 | 0.755561 | 4.569380 |
| 3 | 1.988501 | 0.194496 | 1.284499 | 0.453752 | 0.456938 | 2.688739 |
| 4 | 1.719627 | 0.195371 | 1.399699 | 0.537847 | 0.268874 | 1.516385 |
| 5 | 1.567989 | 0.185331 | 1.451349 | 0.602529 | 0.151638 | 0.795693 |
| 6 | 1.488420 | 0.170066 | 1.465949 | 0.652719 | 0.079569 | 0.451715 |
| 7 | 1.452291 | 0.152934 | 1.459959 | 0.691966 | 0.039246 | 0.353218 |
| 8 | 1.441603 | 0.135839 | 1.443445 | 0.722858 | 0.030892 | 0.278029 |
| 9 | 1.445156 | 0.119790 | 1.422429 | 0.747310 | 0.024452 | 0.220065 |
| 10 | 1.456087 | 0.105253 | 1.400400 | 0.766753 | 0.022030 | 0.174987 |

取停止准则

```text
||x^(k) - x^(k-1)||_inf < 10^(-8)
```

则在 `k = 3786` 次迭代后得到

```text
x_hat ≈ (1.81999805, -0.35999679, 1.34999920, 0.79000048)^T
```

残差为

```text
||Ax_hat-b_hat||_inf ≈ 6.039727e-8
```

这与列主元高斯消去法所得结果

```text
x_hat = (1.82, -0.36, 1.35, 0.79)^T
```

一致。

## 5. 误差估计与实际误差比较

题目给出的摄动估计式为

```text
||delta_x|| / ||x|| <= cond(A) * (||delta_A|| / ||A|| + ||delta_b|| / ||b||) / (1 - cond(A) * ||delta_A|| / ||A||)
```

本题中矩阵 `A` 没有扰动，即

```text
delta_A = 0
```

因此上式化简为

```text
||delta_x|| / ||x|| <= cond(A) * ||delta_b|| / ||b||
```

### 求条件数

由计算得

```text
A^(-1) =
| 25  -41  10  -6 |
|-41   68 -17  10 |
| 10  -17   5  -3 |
| -6   10  -3   2 |
```

所以

```text
||A||_1      = 33
||A^(-1)||_1 = 136
cond_1(A)    = 33 * 136 = 4488
```

又因为行和最大值也等于 `33`，而 `A^(-1)` 的行和最大值为 `136`，故

```text
||A||_inf      = 33
||A^(-1)||_inf = 136
cond_inf(A)    = 33 * 136 = 4488
```

### 1-范数下比较

```text
||x||_1       = |1|+|1|+|1|+|1| = 4
||delta_x||_1 = |0.82|+|1.36|+|0.35|+|0.21| = 2.74
```

故实际相对误差为

```text
||delta_x||_1 / ||x||_1 = 2.74 / 4 = 0.685
```

再计算右端项相对扰动：

```text
||b||_1       = |32|+|23|+|33|+|31| = 119
||delta_b||_1 = |0.01|+|0.01|+|0.01|+|0.01| = 0.04
```

故

```text
||delta_b||_1 / ||b||_1 = 0.04 / 119 ≈ 3.3613445e-4
```

误差估计上界为

```text
cond_1(A) * ||delta_b||_1 / ||b||_1
= 4488 * 0.04 / 119
= 264 / 175
≈ 1.508571
```

因此

```text
实际相对误差 0.685 < 估计上界 1.508571
```

### 无穷范数下比较

```text
||x||_inf       = 1
||delta_x||_inf = max(0.82,1.36,0.35,0.21) = 1.36
```

故实际相对误差为

```text
||delta_x||_inf / ||x||_inf = 1.36
```

又有

```text
||b||_inf       = 33
||delta_b||_inf = 0.01
```

故

```text
||delta_b||_inf / ||b||_inf = 0.01 / 33 ≈ 3.0303030e-4
```

误差估计上界为

```text
cond_inf(A) * ||delta_b||_inf / ||b||_inf
= 4488 * 0.01 / 33
= 34 / 25
= 1.36
```

因此

```text
实际相对误差 1.36 = 估计上界 1.36
```

### 比较结果汇总

| 范数 | `cond(A)` | `||delta_b|| / ||b||` | 误差估计上界 | 实际相对误差 |
|---|---:|---:|---:|---:|
| `1` | 4488 | `0.04/119 ≈ 3.3613445e-4` | `1.508571` | `0.685000` |
| `∞` | 4488 | `0.01/33 ≈ 3.0303030e-4` | `1.360000` | `1.360000` |

## 6. 结果可靠性评估

由上面的结果可以看出：

1. `b` 的相对扰动只有 `10^(-4)` 量级，非常小；
2. 但解的相对变化却达到 `68.5%` 和 `136%`；
3. 说明这个方程组对右端项扰动非常敏感；
4. 条件数 `cond_1(A) = cond_inf(A) = 4488` 很大，因此该问题是病态的；
5. 特别是在 `∞` 范数下，实际相对误差恰好等于理论上界，说明该误差估计在本题中非常紧。

因此可以认为：

```text
虽然原方程组的精确解为 x = (1,1,1,1)^T，
但该问题对右端项中的微小误差极其敏感。
当 b 发生 0.01 量级扰动时，解会产生很大的变化，
故该问题的数值结果可靠性较差。
```

## 7. 最终答案

```text
原方程组：
x = (1, 1, 1, 1)^T

扰动后方程组：
x_hat = (1.82, -0.36, 1.35, 0.79)^T

1-范数下：
实际相对误差 = 0.685
误差估计上界 = 1.508571

∞-范数下：
实际相对误差 = 1.36
误差估计上界 = 1.36

结论：
该方程组对右端项扰动非常敏感，属于病态问题，
因此所得解的可靠性较差。
```
